<header>
    <a href={{path('welcome')}}>Retour</a>
</header>
<h1>Post creation</h1>

<form action="{{ path('post_admin_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('post_admin') }}">
            Back to the list
        </a>
    </li>
</ul>
